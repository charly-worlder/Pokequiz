# PROJ-1: Benutzerkonto & Login

## Dependencies
- Keine

## User Stories
- Als deutschsprachiger Pokémon-Fan möchte ich mich mit E-Mail, Passwort und einem eindeutigen Trainernamen registrieren, damit ich sofort mitspielen und auf der Rangliste erscheinen kann.
- Als wiederkehrender Nutzer möchte ich mich mit E-Mail und Passwort einloggen, damit ich meine bisherigen Ergebnisse und meinen Platz auf der Rangliste behalte.
- Als Nutzer, der sein Passwort vergessen hat, möchte ich es per E-Mail-Link zurücksetzen können, damit ich nicht dauerhaft ausgesperrt bin.
- Als eingeloggter Nutzer möchte ich mich abmelden können, damit mein Konto auf einem geteilten Gerät nicht offen bleibt.
- Als Betreiber der öffentlichen Rangliste möchte ich, dass Logins gegen Brute-Force-Versuche geschützt sind, damit die Rangliste echte Ergebnisse zeigt.

## Out of Scope
- **Trainername ändern** — bewusst dauerhaft fix, siehe Decision Log. Kein Profilbereich im MVP.
- **E-Mail-Verifizierung / Bestätigungslink** — bewusst weggelassen, siehe Decision Log.
- **Login über Drittanbieter** (Google, Apple, etc.) — nur E-Mail/Passwort im MVP.
- **Konto-Löschung und Datenexport** — gehört zu PROJ-4 (Datenschutz & Kontolöschung).
- **Rollen oder Berechtigungsstufen** — alle angemeldeten Nutzer sind gleichberechtigt (siehe `docs/app-shell.md`).
- **Profilseite** über den Passwort-Reset hinaus.
- **CAPTCHA bei der Registrierung** — bewusst nicht im MVP, siehe Decision Log. Wird bei tatsächlich beobachtetem Missbrauch nachgerüstet, nicht präventiv gebaut. **Der Preis dieser Entscheidung steht als EC-9 im Vertrag:** Ohne CAPTCHA muss der Konto-Zähler weit gefasst bleiben, und ein bekanntes Konto ist phasenweise blockierbar. Käme je ein CAPTCHA hinzu, gehört AC-16 enger gestellt.

## Acceptance Criteria

- [ ] **AC-1** — Angenommen ein Besucher ist ausgeloggt und öffnet `/login`, wenn er E-Mail, Passwort (mind. 8 Zeichen) und Trainername (3–20 Zeichen, nur Buchstaben/Zahlen/Unterstrich) ausfüllt und absendet, dann wird ein Konto samt Profil mit diesem Trainernamen angelegt und er ist sofort eingeloggt — ohne E-Mail-Bestätigung.
- [ ] **AC-2** — Angenommen ein Trainername ist (unabhängig von Groß-/Kleinschreibung) bereits vergeben, wenn sich jemand damit registrieren will, dann wird die Registrierung mit einer Fehlermeldung am Trainername-Feld abgelehnt.
- [ ] **AC-3** — Angenommen eine E-Mail-Adresse ist bereits registriert, wenn sich jemand erneut damit registrieren will, dann zeigt das Formular die Meldung „Diese E-Mail-Adresse ist bereits registriert" mit einem Link zum Login.
- [ ] **AC-4** — Angenommen ein Nutzer hat ein bestehendes Konto, wenn er auf `/login` E-Mail und Passwort korrekt eingibt und absendet, dann wird er eingeloggt und zur Startseite (`/`) weitergeleitet.
- [ ] **AC-5** — Angenommen ein Nutzer ist eingeloggt, wenn er den Browser schließt und später zurückkehrt, dann ist er weiterhin eingeloggt, bis er sich aktiv abmeldet.
- [ ] **AC-6** — Angenommen ein eingeloggter Nutzer, wenn er auf „Abmelden" klickt, dann wird die Sitzung beendet und er landet auf `/login`.
- [ ] **AC-7** — Angenommen ein Login-Versuch schlägt fehl (falsches Passwort oder unbekannte E-Mail-Adresse), wenn die Fehlermeldung angezeigt wird, dann ist sie in beiden Fällen identisch und verrät nicht, ob die E-Mail-Adresse existiert.
- [ ] **AC-8** — Angenommen von derselben Verbindung gab es innerhalb einer Minute 5 fehlgeschlagene Login- oder Registrierungsversuche (beide Vorgänge zählen getrennt), wenn ein weiterer Versuch erfolgt, dann wird er von der App-eigenen Drosselung abgewiesen, **bevor** die Zugangsdaten geprüft werden; ein **erfolgreicher** Login erstattet den einen Versuch, den er selbst verbraucht hat, sodass im Ergebnis nur Fehlversuche zählen. **Diese Zusage gilt nur, wenn die anfragende IP-Adresse überhaupt verlässlich feststellbar ist** — der Host muss `x-forwarded-for` selbst setzen und einen vom Client mitgebrachten Wert verwerfen; ohne das fallen alle Spieler auf einen gemeinsamen Sammelschlüssel und der Schutz wird zur Aussperr-Waffe gegen die eigenen Nutzer (Deploy-Blocker BUG-61 in `features/INDEX.md`). Supabases eingebaute Rate-Limit-Regel bleibt als zweite Ebene bestehen, ist aber ausdrücklich **nicht** der Schutz.
- [ ] **AC-10** — Angenommen ein Nutzer hat sein Passwort vergessen, wenn er auf `/login` „Passwort vergessen?" wählt und seine E-Mail-Adresse eingibt, dann erhält er, falls ein Konto zu dieser Adresse existiert, einen Reset-Link per E-Mail; die angezeigte Meldung ist dieselbe unabhängig davon, ob die Adresse existiert.
- [ ] **AC-11** — Angenommen ein Nutzer öffnet einen gültigen Passwort-Reset-Link, wenn er ein neues Passwort (mind. 8 Zeichen) setzt und absendet, dann wird das Passwort geändert und er kann sich damit einloggen.
- [ ] **AC-12** — Angenommen ein Nutzer öffnet einen abgelaufenen oder bereits verwendeten Reset-Link, wenn er versucht ein neues Passwort zu setzen, dann wird eine Fehlermeldung angezeigt und er kann einen neuen Link anfordern.
- [ ] **AC-13** — Angenommen ein Nutzer lässt ein Pflichtfeld im Registrierungs- oder Login-Formular leer, wenn er absendet, dann wird für jedes fehlende Pflichtfeld eine Validierungsfehlermeldung angezeigt, ohne dass die Seite neu lädt.
- [ ] **AC-14** — Angenommen das Registrierungsformular wird angezeigt, wenn der Nutzer es ausfüllt, dann sieht er vor dem Absenden den Hinweis auf die Datenschutzerklärung; dieser Hinweis trägt einen Link **genau dann, wenn die Zielseite existiert**, und bleibt bis dahin unverlinkter Text statt eines toten Links (dieselbe Regel, die `docs/app-shell.md` für die Fußzeile aufstellt).
- [ ] **AC-15** — Angenommen das Registrierungsformular wird angezeigt, wenn der Nutzer das Trainername-Feld ausfüllt, dann steht direkt an diesem Feld — sichtbar ohne Klick, Aufklappen oder Überfahren mit der Maus — der Hinweis „Dein Trainername ist für alle Spieler auf der Bestenliste sichtbar und kann später nicht mehr geändert werden."; eine Bestätigung durch den Nutzer wird nicht verlangt.

<!-- AC-16 bis AC-18 nachgetragen am 2026-09-05 durch /refine PROJ-1. Sie beschreiben
     keinen neuen Umfang, sondern Schutzmechanismen, die seit BUG-29/39/47/54 gebaut
     und gemessen sind, aber in keinem Kriterium standen. -->

- [ ] **AC-16** — Angenommen gegen dieselbe E-Mail-Adresse gab es innerhalb von 15 Minuten 20 Login-Versuche, wenn ein weiterer erfolgt, dann wird er abgewiesen — **unabhängig davon, von welcher Verbindung er kommt**. Ein erfolgreicher Login leert diesen Zähler vollständig, damit Tippfehler den rechtmäßigen Besitzer nichts kosten.
- [ ] **AC-17** — Angenommen von derselben Verbindung gab es innerhalb von 5 Minuten 3 Anfragen für einen Passwort-Reset, wenn eine weitere erfolgt, dann wird sie abgewiesen. Anders als beim Login wird hier **nichts erstattet**: Begrenzt ist der Versand selbst, nicht das Raten — dieselbe Abgrenzung gilt für die Registrierung, wo eine Erstattung die Massenanlage von Konten unbegrenzt machen würde.
- [ ] **AC-18** — Angenommen die Drosselung kann einen Versuch nicht zählen (Zähler nicht erreichbar, Zugangsschlüssel fehlt), wenn jemand sich anmelden, registrieren oder ein Passwort zurücksetzen will, dann wird der Versuch **abgelehnt** statt durchgelassen. Eine Drosselung, die im Fehlerfall stillschweigend aufmacht, ist genau dann nicht da, wenn etwas nicht stimmt.

## Edge Cases
- **EC-1** — Zwei Registrierungen mit demselben Trainernamen (unterschiedliche Groß-/Kleinschreibung) treffen gleichzeitig ein: Die zuerst verarbeitete gewinnt, die zweite bekommt die Fehlermeldung „Trainername bereits vergeben".
- **EC-2** — Ein ausgeloggter Nutzer ruft eine geschützte Route (z. B. `/`) direkt per URL auf: Er wird zu `/login` weitergeleitet.
- **EC-3** — Jemand fordert einen Passwort-Reset für eine nicht existierende E-Mail-Adresse an: Er sieht dieselbe „E-Mail wurde verschickt, falls ein Konto existiert"-Meldung wie bei einer existierenden Adresse.
- **EC-4** — Eine Verbindung oder ein Konto hat seine Grenze erreicht und versucht es erneut, bevor das Fenster abgelaufen ist: Der Versuch wird weiterhin abgewiesen, mit einer Meldung, die **nicht** verrät, ob zu der Adresse ein Konto existiert (AC-7 gilt auch hier). Die Fenster heilen von selbst aus — 60 Sekunden bei der Verbindung, 15 Minuten beim Konto; niemand muss freigeschaltet werden.
- **EC-5** — Ein Trainername enthält nicht erlaubte Zeichen (z. B. Leerzeichen, Emoji) oder ist kürzer als 3 / länger als 20 Zeichen: Die Registrierung wird mit einer feldspezifischen Fehlermeldung abgelehnt.
- **EC-6** — Die Verbindung zu Supabase Auth schlägt beim Absenden fehl (Netzwerkfehler): Der Nutzer sieht eine Fehlermeldung mit einer „Erneut versuchen"-Möglichkeit, ohne bereits eingegebene Werte zu verlieren.
- **EC-7** — Angenommen ein Nutzer fordert den Passwort-Reset auf einem Gerät an und öffnet den Link auf einem anderen (Anfrage am Rechner, Mail am Handy), wenn er den Link öffnet, dann kann er dort ein neues Passwort setzen: Der Link ist an das Konto gebunden, nicht an das Gerät oder den Browser, in dem er angefordert wurde.

> **EC-8 und EC-9 beschreiben Verhalten, das unerwünscht, aber bewusst so entschieden ist.** Beide sind gemessen, verstanden und absichtlich nicht behoben. Sie stehen im Vertrag, damit niemand sie später für ein Versehen hält und beiläufig „repariert" — und damit ein QA-Lauf sie als erwartetes Verhalten wiedererkennt statt als neuen Befund. **Beide hängen an derselben Stellschraube:** wie die erlaubten Versuche zwischen Verbindungs-Zähler (AC-8) und Konto-Zähler (AC-16) verteilt sind. Wer an einer davon dreht, muss beide zusammen neu rechnen — und zwar für die unbequemste realistische Nutzung: einen Schulanschluss am Montagmorgen, nicht einen Haushalt ohne Tippfehler. Die vollständige Abwägung steht in `design.md` → „Bewusst akzeptierte Risiken der Drosselung".

- **EC-8** — Mehrere Spieler teilen sich eine öffentliche IP-Adresse (Haushalt, Schule, Büro-NAT, Mobilfunk-CGNAT): Solange sich niemand vertippt, kommen **alle** hinein, weil der erfolgreiche Login seinen Versuch erstattet (gemessen: 8 von 8, dazu 25 Anmelde-/Abmelde-Zyklen ohne eine einzige Abweisung). Sammeln sich dagegen 5 **Fehl**versuche pro Minute an, ist die Verbindung für alle dicht — auch für die, die richtig tippen (gemessen: bei je einem Tippfehler kommen 4 von 8 hinein). **Bewusst akzeptiert** (BUG-55): Das Fenster heilt nach 60 Sekunden aus, es gibt einen Workaround (warten), und jede Gegenmaßnahme schwächt den einzigen Schutz gegen Passwort-Spraying.
- **EC-9** — Jemand kennt die E-Mail-Adresse eines Spielers und schickt 20 Fehlversuche je 15 Minuten dagegen: Der rechtmäßige Besitzer wird danach auch mit **richtigem** Passwort und von einer völlig unbelasteten Verbindung abgewiesen (gemessen: 20 Fehlversuche von 20 verschiedenen Adressen kommen durch, danach ist der Besitzer draußen). **Bewusst akzeptiert** (BUG-57): Das Gegenmittel wäre ein CAPTCHA, das dieses Produkt bewusst nicht hat (siehe Out of Scope). Ein Konto ist damit phasenweise blockierbar, nie dauerhaft — das Fenster heilt aus.

> **EC-10 ist ebenfalls ein bewusst akzeptiertes Risiko, hängt aber an einer anderen Stelle als EC-8 und EC-9** — nicht an der Drosselung, sondern an AC-7 und daran, was ein fremder Dienst über seine Antwortzeit preisgibt. Es steht hier aus demselben Grund im Vertrag: damit es beim nächsten Sicherheits-Durchgang als bekannt und entschieden wiedererkannt wird und nicht als frischer Befund.

- **EC-10** — Jemand liest nicht die Fehlermeldung, sondern misst die Antwortzeit: Ein bestehendes Konto mit falschem Passwort antwortet messbar langsamer als eine unbekannte Adresse (gemessen am 2026-09-05, je 12 Anfragen: **Median 176 ms gegenüber 102 ms**, rund 74 ms Unterschied), weil nur der bestehende Fall durch die Passwort-Prüfung läuft. Die Kontoexistenz ist damit ermittelbar, obwohl AC-7 die Meldung wortgleich hält. **Bewusst akzeptiert** (BUG-65): Die Ursache liegt in Supabases Auth-Dienst, nicht im eigenen Code — die App ruft nur `signInWithPassword` auf und könnte die Differenz nur glätten, indem sie **jede** Anmeldung künstlich verzögert, also den schnellen Rundenstart aus `docs/PRD.md` für alle bezahlt. Der Gewinn wäre gering: Was ein Angreifer mit dem Wissen anfangen kann, begrenzt der Konto-Zähler (AC-16).

## Technical Requirements (optional)
- Auth-Formulare senden ausschließlich per POST, nie per GET (siehe `.claude/rules/security.md`)
- Passwörter werden ausschließlich gehasht gespeichert (Supabase-Auth-Standard), nie im Klartext
- Datenregion: eu-central-1 (Frankfurt), siehe `docs/PRD.md` → Rahmenbedingungen
- Die Drosselung der Zugangsdaten-Pfade (AC-8, AC-16, AC-17, AC-18) sitzt **in den Server Actions selbst**, nicht im Proxy und nicht im UI: Next.js bindet eine Server Action nicht an die Route, auf der sie definiert wurde — eine Schranke am Pfad wäre über eine beliebige andere Route umgehbar (BUG-30, BUG-31)
- Die Zähler liegen in Postgres; Hochzählen und Prüfen geschehen in **einem** Schritt, damit gleichzeitige Anfragen sich nicht überholen
- Supabases eingebaute Rate-Limit-Regel bleibt als zweite Ebene aktiv, ist aber **Untergrenze, nicht der Schutz**: Weil die App ausschließlich über Server Actions mit Supabase spricht, sieht Supabase für alle Spieler dieselbe Server-IP (BUG-21)
- Die Schlüssel der Konto-Drosselung enthalten E-Mail-Adressen; ihre Aufbewahrung und ihre Löschung mit dem Konto regelt `docs/privacy.md`

## Open Questions
- [x] Braucht es einen Hinweis, der Nutzer davon abhält, ihren echten Namen als Trainernamen zu verwenden? → **Ja — als AC-15 in den Vertrag aufgenommen** (2026-09-03), gebaut wird er in `T14`. Aufgedeckt durch `/dsgvo PROJ-3`: Bis dahin wurde der Trainername nirgends angezeigt, mit der Weltrangliste erscheint er erstmals vor anderen Menschen — und ist dann laut Produktentscheidung unveränderlich. Der Hinweis nennt beide Hälften (sichtbar **und** endgültig) und verlangt keine Bestätigung, weil die Rechtsgrundlage Vertrag ist, nicht Einwilligung.
- [ ] Greift Art. 8 DSGVO (Einwilligung von Kindern) hier überhaupt, wo die Kontoerstellung über Vertrag (Art. 6(1)(b)) läuft — oder ist die relevante Frage stattdessen die Geschäftsfähigkeit nach BGB? Und sollte der Trainername mindestens einmalig änderbar sein, damit ein Kind, das seinen echten Namen eingetragen hat, ihn ohne Löschung des ganzen Kontos zurücknehmen kann? AC-15 warnt vorher, löst diese Frage aber nicht (siehe `docs/privacy.md` → Für einen Anwalt)
- [ ] Hosting-Anbieter (Vercel/Hostinger) noch nicht entschieden — betrifft die Verarbeitung von Server-Logs, wird bei `/deploy` festgelegt
- [x] AC-8 (Supabases eingebautes Rate-Limit) ließ sich lokal nicht auslösen — 150 Fehlversuche gegen die lokale Supabase-Instanz lösten kein 429 aus. → **Doppelt erledigt** (2026-09-05): Gegen das gehostete Projekt gemessen, 429 bei Versuch 33 (BUG-7 geschlossen am 2026-09-04). Vor allem aber hängt AC-8 seit BUG-29 gar nicht mehr an dieser Regel, sondern an der App-eigenen Drosselung — die ist lokal auslösbar, gemessen und durch `tests/PROJ-1-throttle.spec.ts` abgesichert. Supabases Regel steht nur noch als zweite Ebene
- [ ] AC-11/AC-12 (Passwort-Reset-Link): Die Lösung — Sitzung wird clientseitig aus dem URL-Fragment gelesen, keine Server-seitige Code-Exchange — hängt von der aktuellen lokalen `flow_type`-Konfiguration ab und muss nach dem ersten Cloud-Deploy erneut verifiziert werden, zusammen mit dem AC-8-Test. Außerdem müssen Site URL und Redirect URLs im gehosteten Supabase-Projekt auf die echte Domain gesetzt werden (siehe design.md → Settings the user makes) — sonst tritt derselbe Bug wie lokal erneut auf

## Decision Log

### Product Decisions
| Decision | Rationale | Date |
|----------|-----------|------|
| Ein Formular registriert Trainername gemeinsam mit E-Mail/Passwort | Reduziert Reibung, passt zum Ziel „sofort loslegen" | 2026-08-31 |
| Keine E-Mail-Verifizierung vor dem ersten Spiel | Rundenstart soll ohne Zwischenschritt möglich sein; passt zum Erfolgskriterium „Runde startet in unter 3 Sekunden" | 2026-08-31 |
| Trainername: 3–20 Zeichen, Buchstaben/Zahlen/Unterstrich, Groß-/Kleinschreibung wird bei der Eindeutigkeit ignoriert | Verhindert Verwechslungen auf der Rangliste und unleserliche Namen | 2026-08-31 |
| Trainername ist nach der Registrierung dauerhaft fix | Vermeidet Verwirrung auf der Rangliste, kein zusätzlicher Profil-Scope im MVP | 2026-08-31 |
| Passwort-Mindestlänge 8 Zeichen, keine erzwungene Zeichen-Vielfalt | Aktuelle Empfehlung (NIST/OWASP), weniger Reibung als klassische Komplexitätsregeln | 2026-08-31 |
| Passwort-Reset gehört zum MVP-Umfang | Ohne Self-Service-Reset sperren sich Nutzer dauerhaft aus | 2026-08-31 |
| Session bleibt bis aktivem Logout bestehen (kein Ablauf nach Inaktivität) | Passt zu einem Spiel, das man wiederholt kurz öffnet | 2026-08-31 |
| ⚠️ **ÜBERHOLT am 2026-09-05 — siehe die fünf Zeilen am Ende dieser Tabelle.** Login-/Registrierungs-Drosselung: Supabases eingebaute IP-Regel (Standardwert 30 Versuche / 5 Minuten), keine eigene kontobezogene Drosselung | Dieselbe reaktive Logik wie beim CAPTCHA-Verzicht: bewusst kein Sicherheits-Feature vorab bauen, das erst bei tatsächlichem Missbrauch gebraucht wird; eine eigene Drosselung kann jederzeit nachgerüstet werden | 2026-08-31 |
| Kein CAPTCHA bei der Registrierung im MVP; AC-9 (ursprünglich: CAPTCHA gegen automatisierte Registrierung) dafür gestrichen | Bewusste produkttypische Entscheidung: reaktiv nachrüsten, wenn tatsächlich Missbrauch auftritt, statt präventiv zu bauen — unabhängig von der geringen erwarteten Nutzerzahl | 2026-08-31 |
| Kein Leaked-Password-Schutz (HaveIBeenPwned) im MVP | Dieselbe reaktive Logik wie beim CAPTCHA-Verzicht; zusätzlich ein Supabase-Paid-Plan-Feature | 2026-08-31 |
| Bei Duplikat-E-Mail wird offen mitgeteilt, dass das Konto existiert (statt neutraler Meldung) | Anders als beim Login wird hier kein Passwort erraten; der Nutzer muss erfahren, dass er einloggen statt erneut registrieren soll | 2026-08-31 |
| Anmeldung und Registrierung teilen sich die Route `/login` mit einem Umschalter | Passt zur App-Shell-Karte, die genau eine Route „Anmeldung" vorsieht | 2026-08-31 |
| AC-14 verlangt den Datenschutz-Link erst, wenn die Zielseite existiert — vorher unverlinkter Text | Der Link zeigte auf eine 404 (BUG-10). Die alte Formulierung verlangte einen sichtbaren Link, ohne je zu verlangen, dass er irgendwohin führt — im Wortlaut erfüllt, in der Absicht verfehlt. Eine Platzhalter-Seite zu bauen wäre die schlechteste Lösung: Sie behauptet eine Datenschutzerklärung, die es nicht gibt, und das ist rechtlich schlimmer als eine 404. `docs/app-shell.md` hat für die Fußzeile längst dieselbe Regel; PROJ-1 folgt ihr jetzt auch. PROJ-4 setzt den Link, wenn es die Seite baut | 2026-09-03 |
| Der Passwort-Reset muss geräteübergreifend funktionieren (EC-7) — die Einschränkung wird gebaut, nicht dokumentiert | Die Mail am Handy zu öffnen, während man am Rechner sitzt, ist der Normalfall, nicht der Randfall. Weil es bewusst keine E-Mail-Bestätigung gibt, ist der Reset der **einzige** Weg zurück ins Konto: Schlägt er fehl, ist der Nutzer dauerhaft ausgesperrt. AC-11 schließt fremde Geräte ohnehin nicht aus — die Einschränkung nur zu dokumentieren hätte eine bestehende Zusage nachträglich verkleinert, statt eine Lücke zu beschreiben. Erschwerend: Die App meldete dabei „Link ungültig oder abgelaufen" über einen frischen, gültigen Link (BUG-6) | 2026-09-03 |
| Hinweis am Trainername-Feld (AC-15) als reiner Hinweistext, **ohne** Bestätigungs-Checkbox | Die Rechtsgrundlage ist Vertrag (Art. 6(1)(b) DSGVO), nicht Einwilligung. Eine Pflicht-Checkbox würde eine Einwilligung inszenieren, die hier gar nicht trägt — und eine, die man nicht verweigern kann, ohne das Produkt zu verlieren, wäre nach Art. 7 ohnehin keine wirksame. Sie kostet Reibung auf dem Weg, den das PRD ausdrücklich reibungsarm haben will, und liefert juristisch nichts | 2026-09-03 |
| Der Hinweis nennt beide Hälften in einem Satz: sichtbar für andere **und** später nicht änderbar | „Öffentlich" allein verschweigt genau den Teil, der die Entscheidung unumkehrbar macht. Der bereits vorhandene Datenschutz-Link (AC-14) hat den Fall nicht abgedeckt — ein Hinweis, den man erst auf einer Unterseite findet, erreicht niemanden im Moment der Eingabe | 2026-09-03 |
| Die Drosselung ist eine **App-eigene, zweiteilige** Sperre (Verbindung **und** Konto) statt Supabases eingebauter Regel | Die reaktive Haltung vom 2026-08-31 („nachrüsten, wenn Missbrauch auftritt") wurde von zwei Befunden eingeholt: BUG-29 zeigte 35 Fehlversuche gegen ein bestehendes Konto ohne jede Sperre, und BUG-21 zeigte, warum Supabases Regel hier prinzipiell nicht greifen kann — die App spricht nur über Server Actions mit Supabase, das sieht für alle Spieler dieselbe IP. Damit war die eigene Bedingung erfüllt, nicht verworfen | 2026-09-05 |
| Der Konto-Zähler (AC-16, 20 / 15 min) ist bewusst **weiter** gefasst als der Verbindungs-Zähler (AC-8, 5 / min) | Ein enger Konto-Zähler stoppt verteiltes Raten wirksam — und macht zugleich jedes Konto billig aussperrbar. Ohne CAPTCHA ist das der gewählte Mittelweg: zu wenig zum Erraten eines Passworts, zu viel, um jemanden mit einem Skript billig lahmzulegen | 2026-09-05 |
| Ein erfolgreicher Login **erstattet seinen eigenen Versuch**, statt den Verbindungs-Zähler zu leeren | Beide naheliegenden Varianten sind gemessen gescheitert: Geleert ist die Bremse von jedem abschaltbar, der ein einziges eigenes Konto besitzt (BUG-39 — 24 Rateversuche in 3,6 s ungebremst); gar nicht erstattet kostet jeder Erfolg einen geteilten Anschluss Budget (BUG-54 — nur 5 von 8 Spielern kommen hinein). Die Erstattung löst beides: Gezählt werden im Ergebnis nur Fehlversuche | 2026-09-05 |
| Erstattet wird **ausschließlich beim Login**, nie bei Registrierung oder Passwort-Reset (AC-17) | Dort ist die begrenzte Sache die Handlung selbst — Konten anlegen, Mails verschicken —, nicht das Raten. Mit Erstattung wäre die Massenanlage von Konten unbegrenzt (BUG-47), beim Reset wäre es ein Versand-Vektor | 2026-09-05 |
| Die Zeitdifferenz beim Login (EC-10) wird **hingenommen statt geglättet** | Sie entsteht in Supabases Auth-Dienst, nicht im eigenen Code. Die einzige App-seitige Gegenmaßnahme wäre, jede Anmeldung auf eine feste Mindestdauer zu strecken — das verlangsamt den Weg ins Spiel für alle Spieler, um einem Angreifer eine Auskunft zu nehmen, die der Konto-Zähler ohnehin schwer verwertbar macht. Der Wortlaut von AC-7 bleibt erfüllt; die Lücke steht als EC-10 offen im Vertrag statt in einer Fußnote | 2026-09-05 |
| Die **Grenzen** des Schutzes stehen als EC-8/EC-9 im Vertrag, nicht nur in `design.md` | Ein Schutz, dessen Lücken nur im Design stehen, wird beim nächsten QA-Lauf erneut als Befund gemeldet und beim nächsten Umbau beiläufig „repariert". Im Vertrag sind sie prüfbar, und ihre Änderung ist eine bewusste Entscheidung statt eines Nebeneffekts | 2026-09-05 |
